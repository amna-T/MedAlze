services:
  - type: web
    name: medalze-backend
    runtime: python
    plan: free
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -w 1 app:app
    envVars:
      - key: FLASK_APP
        value: app.py
      - key: FLASK_ENV
        value: production
      - key: MODEL_PATH
        value: model/chexnet.pth
      - key: GEMINI_API_KEY
        sync: false
